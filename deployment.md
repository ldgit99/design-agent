# Deployment Checklist

## 1. 목적

이 문서는 Human-AI 오케스트레이션 기반 수업 설계 에이전트를 GitHub와 Vercel을 통해 배포하고 운영할 때 필요한 최소 체크리스트를 정리한다. 배포는 단순 배포 성공이 아니라, 보안, 검증, 실험 분리, 운영 추적이 함께 성립해야 한다.

---

## 2. 저장소 준비

- GitHub 저장소 생성
- 기본 브랜치 `main` 설정
- 통합 브랜치 `develop` 생성
- 보호 규칙이 필요한 경우 `main`에 branch protection 적용
- `.gitignore`에 환경 변수 파일 포함 여부 확인
- 민감 정보가 커밋되지 않았는지 확인

권장 브랜치:

- `main`
- `develop`
- `feature/*`
- `experiment/*`

---

## 3. 필수 환경 변수

아래 값은 로컬, Preview, Production을 분리해 관리한다.

- `OPENAI_API_KEY`
- `OPENAI_MODEL`
- `DATABASE_URL`
- `VECTOR_DB_URL`
- `VECTOR_DB_API_KEY`
- `APP_ENV`
- `LOG_LEVEL`

규칙:

- OpenAI API 키는 서버 환경 변수로만 저장
- 브라우저 코드에서 직접 참조 금지
- 실험용 키와 운영용 키 분리 권장

---

## 4. GitHub 체크리스트

### PR 전

- 기능 브랜치 생성
- 관련 문서 업데이트 여부 확인
- 테스트 추가 여부 확인
- 환경 변수 추가가 있으면 문서화

### PR 시

- 변경 목적을 명확히 설명
- 연구 실험 브랜치인지 운영 기능 브랜치인지 표시
- UI 변경 시 스크린샷 또는 Preview 링크 첨부

### 병합 전

- 테스트 통과
- 타입 체크 통과
- 린트 통과
- 민감 정보 노출 없음 확인
- API 응답 구조 변화 검토

---

## 5. GitHub Actions 권장 항목

- 테스트 실행
- 타입 체크
- 린트
- 카드 스키마 검증
- 루브릭 JSON 검증
- 보안 스캔

배포 직전 실패를 줄이기 위해 최소한 테스트, 타입 체크, 린트는 기본 파이프라인에 포함한다.

---

## 6. Vercel 프로젝트 설정

### 연결

- GitHub 저장소를 Vercel 프로젝트에 연결
- Production 브랜치를 `main`으로 설정
- Preview 배포를 브랜치별로 자동 생성

### 런타임

- Next.js 프론트엔드 설정 확인
- FastAPI 엔트리포인트 위치 확인
- 서버 호출이 OpenAI 키를 안전하게 참조하는지 확인

### 환경 변수 등록

- Development
- Preview
- Production

환경 변수 변경 후에는 새 배포가 필요하다.

---

## 7. 배포 전 검증

### 기능 검증

- 교사 입력 폼 제출 가능
- intent parsing 정상 동작
- 카드 추천 정상 동작
- 시나리오 생성 정상 동작
- 루브릭 생성 정상 동작
- 교사 수정 및 승인 흐름 동작

### 보안 검증

- 클라이언트에 API 키 노출 없음
- 서버 로그에 민감 정보 노출 없음
- OpenAI 호출이 서버에서만 수행됨

### 데이터 검증

- 카드 스키마 유효
- 생성 결과 JSON 구조 유효
- 리뷰 로그 저장 정상

---

## 8. Preview Deployment 활용

Preview는 연구 실험과 기능 검증에 적극 활용한다.

활용 예:

- 새로운 카드 추천 전략 A/B 비교
- 루브릭 구조 변경 전후 비교
- 특정 교사 그룹 대상 파일럿 테스트
- 하네스 문서 개정 효과 비교

체크 항목:

- Preview URL 접근 가능
- 실험용 환경 변수가 올바르게 연결됨
- 운영 데이터와 실험 데이터가 혼재되지 않음

---

## 9. Production 배포 기준

아래 기준을 모두 만족할 때만 `main`으로 병합한다.

- 핵심 기능 정상
- 테스트 통과
- 고위험 보안 이슈 없음
- 개인정보 처리 위험 없음
- 교사 승인 흐름 정상
- 연구팀 또는 운영팀 검토 완료

---

## 10. OpenAI API 운영 체크

- 호출 실패 시 재시도 정책 적용
- rate limit 대응 전략 존재
- 응답 지연 시 타임아웃 처리 존재
- 동일 입력에 대한 불필요한 재호출 최소화
- 캐시 가능한 카드 검색은 캐시 사용
- 비용 모니터링 지표 확보

---

## 11. 운영 로그와 모니터링

최소한 아래 이벤트는 기록한다.

- 프로젝트 생성
- intent parsing 호출
- 카드 추천 호출
- 시나리오 생성 호출
- 루브릭 생성 호출
- 교사 수정 저장
- 최종 승인
- 에러 발생

민감 정보는 마스킹한다.

---

## 12. 배포 후 확인

- Production URL 정상 응답
- 환경 변수 정상 반영
- 핵심 API 정상 호출
- 로그 수집 정상
- 오류 알림 체계 정상

---

## 13. 권장 다음 문서

- [research.md](D:\OneDrive\Agent\design-agent\research.md)
- [architecture.md](D:\OneDrive\Agent\design-agent\architecture.md)
